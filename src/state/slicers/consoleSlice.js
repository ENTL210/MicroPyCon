import { createSlice } from "@reduxjs/toolkit";


const initialState = {
    consoleOutput: [],
}

export const consoleSlice = createSlice({
    name: 'console',
    initialState,
    reducers: {

    }
})

export const {} = consoleSlice.actions